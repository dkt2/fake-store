/**
 * This is typing for the returns of `createAsyncThunk`
 *  as well as being slightly friendlier names
 */
export enum AsyncStatus {
  IDLE = "IDLE",
  LOADING = "LOADING",
  FAILED = "FAILED",
}
